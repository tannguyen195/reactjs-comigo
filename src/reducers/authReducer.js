import * as types from '../actions/types';


const initial = {

}
const authReducer = (state = initial, action) => {
    switch (action.type) {

        default:
            return state;
    }
};

export default authReducer;
